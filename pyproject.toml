[project]
name = "qwen38-modal-deploy"
version = "0.1.0"
description = "Modal deployment tooling for Qwen3.8-27B on SGLang"
requires-python = ">=3.10,<3.15"
dependencies = [
    "modal[api-proxy-support]==1.5.3",
    "fastapi==0.116.1",
    "httpx==0.28.1",
]
